module.exports = {
  siteUrl: process.env.SITE_URL || "https://www.souvenirlilin.id",
  generateRobotsTxt: true, // (optional) Generate `robots.txt` alongside your `sitemap.xml`
  // ...other options
};
